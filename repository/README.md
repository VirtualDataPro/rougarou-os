# Repository source files

`scripts/repo.py` creates signed APT snapshots and promotes them explicitly.
This directory holds public policy/examples only; generated repository trees
and GPG private keys belong outside the source checkout.

Read [the update policy and commands](../docs/updates.md). The evidence example
is a schema demonstration, not a completed approval or passing test result.
